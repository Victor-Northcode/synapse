<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Напоминания: локальные уведомления раз в сутки. -->
    <uses-permission
        android:name="android.permission.POST_NOTIFICATIONS"/>
    <!-- Реклама за награду (AdMob). -->
    <uses-permission
        android:name="android.permission.INTERNET"/>
    <uses-permission
        android:name="com.google.android.gms.permission.AD_ID"/>
    <application
        android:label="SYNAPSE"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:networkSecurityConfig="@xml/network_security_config">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:resizeableActivity="true"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme"/>
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- ТЕСТОВЫЙ app id AdMob (официальный от Google).
             Перед релизом заменить на собственный. -->
        <meta-data
            android:name="com.google.android.gms.ads.APPLICATION_ID"
            android:value="ca-app-pub-3940256099942544~3347511713"/>

        <!-- Play Games Services (лидерборды). Project ID из Play Console →
             Play Games Services → Configuration. Пока стоит заглушка —
             код не обращается к сервису, пока не заполнен id таблицы
             в lib/core/leaderboard.dart. -->
        <meta-data
            android:name="com.google.android.gms.games.APP_ID"
            android:value="@string/game_services_project_id"/>
        <meta-data
            android:name="flutterEmbedding"
            android:value="2"/>
    </application>
    <queries>
        <intent>
            <action
                android:name="android.intent.action.PROCESS_TEXT"/>
            <data
                android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>